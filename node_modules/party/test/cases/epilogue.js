
/**
 * Module dependencies.
 */

var assert = require('assert')
  , helpers = require('../helpers');

exports.boundary = 'simple-boundary';

exports.test = function(parser, next){
  parser.on('part', function(part){
    part.header.should.have.property('content-type', 'text/plain; charset=us-ascii');
    var buf = '';
    part.on('data', function(chunk){ buf += chunk; });
    part.on('end', function(){
      helpers.assert(buf, 'epilogue');
      next();
    });
  });
};