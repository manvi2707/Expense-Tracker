
var str = 'here is a simple example.';

var pat = 'example'
  , plen = pat.length;

var i = 0
  , len = str.length;

while (i + plen <= len) {
  if (~pat.indexOf(str[i])) break;
  console.dir(str.substr(i));
  i += plen;
}
// 
// console.log(i);
// console.dir(str.substr(i));